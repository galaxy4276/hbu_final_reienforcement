# 학습 1: 52f86
- 데이터량 25\*2 = 50
```
config = (
    BCConfig()
    .environment(env="HalfCheetah-v5")
    .framework("torch")
    .learners(
        num_learners=1,
        num_gpus_per_learner=1
    )
    .training(
        train_batch_size_per_learner=4096,
        lr=1e-4,
        num_epochs=10,
    )
    .rl_module(
        model_config=DefaultModelConfig(
            fcnet_hiddens=[512, 512, 256], 
            fcnet_activation="tanh",
            vf_share_layers=False,
            free_log_std=True,
        )
    )
    .env_runners(
        num_env_runners=1,
        observation_filter="MeanStdFilter" 
    )
    .offline_data(
        input_=[data_path],
        input_read_episodes=True,
        input_read_batch_size=128,
        map_batches_kwargs={"concurrency": 2, "num_cpus": 1},
        iter_batches_kwargs={"prefetch_batches": 1},
        dataset_num_iters_per_learner=1,
    )
    .evaluation(
        evaluation_interval=3,
        evaluation_num_env_runners=1,
        evaluation_duration=3,
        evaluation_duration_unit="episodes",
        evaluation_parallel_to_training=True,
    )
)
```
# 학습 2: 5206a
- 데이터량 25\*2 = 50
```
config = (
    CQLConfig()
    .environment(env="HalfCheetah-v5")
    .framework("torch")
    .learners(
        num_learners=1,
        num_gpus_per_learner=1
    )
    .training(
        train_batch_size_per_learner=2048, 
        lr=None,           
        actor_lr=3e-4,     # 행동 결정 신경망 학습률
        critic_lr=3e-4,    # 가치 판단 신경망 학습률
        alpha_lr=3e-4,     # 엔트로피(Alpha) 학습률     
        num_epochs=50,
    )
    .rl_module(
        model_config=DefaultModelConfig(
            fcnet_hiddens=[512, 512, 256], 
            fcnet_activation="tanh",
            vf_share_layers=False,
            # free_log_std=True,
        )
    )
    .env_runners(
        num_env_runners=0,
        observation_filter="MeanStdFilter" 
    )
    .offline_data(
        input_=[data_path],
        input_read_episodes=True,
        input_read_batch_size=128,
        map_batches_kwargs={"concurrency": 2, "num_cpus": 1},
        iter_batches_kwargs={"prefetch_batches": 1},
        dataset_num_iters_per_learner=1,
    )
    .evaluation(
        evaluation_interval=5,
        evaluation_num_env_runners=1,
        evaluation_duration=5,
        evaluation_duration_unit="episodes",
        evaluation_parallel_to_training=True,
    )
)

config.bc_iters = 0           # BC 워밍업 횟수
config.cql_loss_coeff = 1.0   # CQL 보수적 강도
config.twin_q = True          # Twin Q-Learning 사용
```
# 학습 3: 15ebd
- 데이터량 100\*5 = 500
```
config = (
    MARWILConfig()
    .environment(env="HalfCheetah-v5")
    .framework("torch")
    .learners(
        num_learners=1,
        num_gpus_per_learner=1
    )
    .training(
        train_batch_size_per_learner=4096,
        # [MARWIL 핵심 파라미터]
        beta=1.0,  # 높을수록 "잘한 행동"만 까다롭게 골라서 배움 (0.5 ~ 2.0 추천)
        lr=1e-4,   # 학습률 (안정적)
        num_epochs=50, # 데이터 반복 학습
    )
    .rl_module(
        model_config=DefaultModelConfig(
            fcnet_hiddens=[512, 512, 256], 
            fcnet_activation="tanh",
            vf_share_layers=False,
            # free_log_std=True,
        )
    )
    .env_runners(
        num_env_runners=0,
        observation_filter="MeanStdFilter" 
    )
    .offline_data(
        input_=[data_path],
        input_read_episodes=True,
        input_read_batch_size=128,
        map_batches_kwargs={"concurrency": 2, "num_cpus": 1},
        iter_batches_kwargs={"prefetch_batches": 1},
        dataset_num_iters_per_learner=1,
    )
    .evaluation(
        evaluation_interval=5,
        evaluation_num_env_runners=1,
        evaluation_duration=20,
        evaluation_duration_unit="episodes",
        evaluation_parallel_to_training=True,
    )
)
```
# 학습 4: a0a60
- 데이터: 100\*5=500
```
config = (
    BCConfig()
    .environment(env="HalfCheetah-v5")
    .framework("torch")
    .learners(
        num_learners=1,
        num_gpus_per_learner=1
    )
    .training(
        train_batch_size_per_learner=4096,
        lr=1e-4,
        num_epochs=10,
    )
    .rl_module(
        model_config=DefaultModelConfig(
            fcnet_hiddens=[512, 512, 256], 
            fcnet_activation="tanh",
            vf_share_layers=False,
            free_log_std=True,
        )
    )
    .env_runners(
        num_env_runners=1,
        observation_filter="MeanStdFilter" 
    )
    .offline_data(
        input_=[data_path],
        input_read_episodes=True,
        input_read_batch_size=128,
        map_batches_kwargs={"concurrency": 2, "num_cpus": 1},
        iter_batches_kwargs={"prefetch_batches": 1},
        dataset_num_iters_per_learner=1,
    )
    .evaluation(
        evaluation_interval=5,
        evaluation_num_env_runners=1,
        evaluation_duration=20,
        evaluation_duration_unit="episodes",
        evaluation_parallel_to_training=True,
    )
)
```
# 학습 5: 9b3c5
- 데이터: 100\*5=500
```
config = (
    MARWILConfig()
    .environment(env="HalfCheetah-v5")
    .framework("torch")
    .learners(
        num_learners=1,
        num_gpus_per_learner=1
    )
    .training(
        train_batch_size_per_learner=4096,
        # [MARWIL 핵심 파라미터]
        beta=1.0,
        lr=5e-5,
        num_epochs=100,
        vf_coeff=1.0,
    )
    .rl_module(
        model_config=DefaultModelConfig(
            fcnet_hiddens=[512, 512, 256], 
            fcnet_activation="tanh",
            vf_share_layers=False,
            # free_log_std=True,
        )
    )
    .env_runners(
        num_env_runners=0,
        observation_filter="MeanStdFilter" 
    )
    .offline_data(
        input_=[data_path],
        input_read_episodes=True,
        input_read_batch_size=128,
        map_batches_kwargs={"concurrency": 2, "num_cpus": 1},
        iter_batches_kwargs={"prefetch_batches": 1},
        dataset_num_iters_per_learner=1,
    )
    .evaluation(
        evaluation_interval=5,
        evaluation_num_env_runners=1,
        evaluation_duration=20,
        evaluation_duration_unit="episodes",
        evaluation_parallel_to_training=True,
    )
)
```
# 학습 6: 1314c
```
config = (
    MARWILConfig()
    .environment(env="HalfCheetah-v5")
    .framework("torch")
    .learners(
        num_learners=1,
        num_gpus_per_learner=1
    )
    .training(
        train_batch_size_per_learner=4096,
        # [MARWIL 핵심 파라미터]
        beta=0.5,
        lr=1e-4,
        num_epochs=200,
        vf_coeff=1.0,
        grad_clip=1.0,
    )
    .rl_module(
        model_config=DefaultModelConfig(
            fcnet_hiddens=[512, 512, 256], 
            fcnet_activation="tanh",
            vf_share_layers=False,
            # free_log_std=True,
        )
    )
    .env_runners(
        num_env_runners=0,
        observation_filter="MeanStdFilter" 
    )
    .offline_data(
        input_=[data_path],
        input_read_episodes=True,
        input_read_batch_size=128,
        map_batches_kwargs={"concurrency": 2, "num_cpus": 1},
        iter_batches_kwargs={"prefetch_batches": 1},
        dataset_num_iters_per_learner=1,
    )
    .evaluation(
        evaluation_interval=5,
        evaluation_num_env_runners=1,
        evaluation_duration=20,
        evaluation_duration_unit="episodes",
        evaluation_parallel_to_training=True,
    )
)
```