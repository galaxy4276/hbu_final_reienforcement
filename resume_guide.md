# 변경된 파라미터로 학습을 재개하는 방법

새로운 코드를 작성하지 않고 변경된 파라미터로 학습을 계속하는 방법에 대해 문의하셨습니다.
가장 좋은 방법은 기존 스크립트인 **`base/orl_4_Online_finetuning.py`**를 사용하는 것입니다. 이 스크립트는 바로 이러한 목적(가중치 로드 및 학습 계속)을 위해 설계되었습니다.

## 단계

1.  **체크포인트 찾기**: 재개하려는 체크포인트의 경로를 찾습니다 (예: `~/ray_results/halfcheetah_expert_ppo/.../checkpoint_0000X`).
2.  **`base/orl_4_Online_finetuning.py` 수정**:
    *   `offline_checkpoint` 변수(13행)를 해당 체크포인트 경로로 업데이트합니다.
    *   `config`(16-44행)를 **새로운 하이퍼파라미터** (예: `lr`, `train_batch_size` 등)로 업데이트합니다.
3.  **스크립트 실행**:
    ```bash
    python base/orl_4_Online_finetuning.py
    ```

## 작동 원리
이 스크립트는 *새로운* 학습 실행을 생성(새로운 파라미터 허용)하지만 지정된 체크포인트에서 명시적으로 **가중치를 로드**(`learner_group`)합니다. 이는 효과적으로 새로운 설정으로 학습을 "재개"하는 것과 같습니다.

> **참고**: 원본 스크립트에서 `Tuner.restore()`를 사용하려고 하면 파라미터 변경 사항이 되돌려집니다. `orl_4`와 같은 별도의 미세 조정(fine-tuning) 스크립트를 사용하는 것이 올바른 접근 방식입니다.
